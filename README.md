# Как пользоваться гитом

Открываем командную строку через Win + R и вводим cmd
Далее вводим путь куда мы хотим поместить наш проект:

```shell
cd <путь куда вы хотите положить проект>
```

### Импортируйте 

далее сделайте:

```shell
git clone https://github.com/SonLetGo/MountinComplex.git
```

чтобы скопировать репозитрий

### Работа

Откройте проект 1/1.csproj в Visual Studio

Далее когда вы сделали что хотели (добавили форму или изменили) отравьте изменения для остальных через интерфейс:
```shell
Вкладка Git -> Отправить -> Название изменения ->  Зафиксировать все и отправить
```

### Получить изменения 

Для получения что сделали другие надо:
```shell
Вкладка Git -> Получить
```

# Как импортировать БД

Создаем пустую бд moutin_complex в pgAdmin. Надеюсь справитесь без подсказок.

Скачиваем файлик moutin_complex_backup.sql и находим где у нас Postgres обычно он установлен в C:\Program Files\PostgreSQL\16\bin

Открываем cmd и вводим:

```shell
cd  C:\Program Files\PostgreSQL\16\bin
```

и импортируем в Postgres:
```shell
pg_restore.exe --username "postgres" --dbname "mountin_complex" --verbose "<путь до moutin_complex_backup.sql>"
```

После проверям что все импортировалось